# 安全与隐私说明

本项目的设计前提是**私有部署**:所有制度文档与问答数据均在本机处理,不向任何外部服务发送。

## 数据流向

| 环节 | 处理位置 | 是否出网 |
|---|---|---|
| 文档解析(pdf/docx/txt/md/csv) | 本机 Python 进程 | 否 |
| 文本切分与向量化 | 本机 Ollama(bge-m3) | 否 |
| 向量存储 | 本机 ChromaDB(`data/vectors/<kb_id>/`) | 否 |
| 检索与冲突判定 | 本机进程 + 本机 Ollama | 否 |
| 答案生成 | 本机 Ollama(deepseek-r1:7b) | 否 |
| 前端界面 | 本机 `127.0.0.1:8000` | 否 |

> 系统不含任何外部 API 调用;断网环境下功能完整可用。可通过 `data/logs/backend.err.log` 核查
> 全部 HTTP 请求目标均指向本机 Ollama(`127.0.0.1:11434`)。

## 敏感数据处理建议

1. **不要提交运行数据**:`data/` 目录包含知识库内容、向量、原文归档与审计日志,已在 `.gitignore` 中排除;
2. **审计日志含问题原文**:`audit_log` 表会记录问答问题与操作者部门,如需对外提供数据请先脱敏;
3. **受限知识库**:薪酬、绩效等敏感制度建议建库时设为「受限(指定部门)」,避免越权检索;
4. **备份安全**:`data/` 拷贝即完整备份,请按内部数据分级要求存放;
5. **模型文件**:本地模型由 Ollama 管理,不随本仓库分发。

## 报告安全问题

若发现安全或隐私相关问题(例如越权绕过、信息泄露、提示注入导致的数据外泄),
请通过仓库的 **Security → Report a vulnerability** 私密报告,或提交 Issue 时**不要粘贴真实制度内容**。

建议在报告中包含:
- 复现步骤(使用虚构制度文本)
- 期望行为与实际行为
- 相关日志片段(请先脱敏)
