# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与语义化版本。

## [1.0.0] — 2026-09

首个公开版本:面向中小企业内部制度文档的多知识库私有 RAG 智能问答系统。

### 新增 — 制度治理(核心差异化)
- **向量硬隔离**:每个知识库独立 ChromaDB 实例与独立存储目录、独立 `chroma.sqlite3`;
  删库即物理删除该库目录,不影响其它库;旧版单实例布局启动时自动迁移并做数量校验(保留备份可回滚)。
- **块级增量更新与精准删除**:块内容哈希账本,未变更块复用既有向量,仅重嵌入变更块
  (实测五段文档改一段:复用 4/5 块、344 ms vs 全量重嵌入 1628 ms);删除按文档 ID 精准清理并二次校验零残留。
- **跨知识库冲突检测**:多库检索 → 话题初筛 → 双通道判定(本地模型语义判定 + 数值分歧确定性检测)
  → 冲突告警与生成约束;回答分列来源、不得擅自合并。
- **制度版本与生效期治理**:文件编号、生效/失效日期、状态、发布部门、适用范围、替代关系;
  新版本声明替代后旧版自动废止并退出检索;提供版本链与到期提醒;冲突区分「版本差异」与「跨来源冲突」。

### 新增 — 权限、审计与合规
- **部门/角色权限隔离**:知识库可见范围(全员 / 指定部门),覆盖库列表、自动路由、手动检索、
  冲突检测、文档增删改查与库设置;越权返回 403 与明确原因。
- **审计日志**:问答、冲突告警、越权拒绝、文档新增/更新/删除/属性修改、建库/库设置/删库九类留痕,
  支持按类型/知识库/部门/结果查询与统计;非管理员仅见本部门记录。

### 新增 — 检索与质量
- **混合检索**:BM25(中文二元组 + ASCII 词,零分词依赖)+ 向量通道,RRF 融合,
  确定性元数据重排(制度编号/部门/版本时效/条款路径);检索 hit@1 由 0.933 提升至 1.000。
- **拒答门槛校准**:排序分(相对量纲)与门槛分(向量余弦绝对量纲)分离,阈值经评测集校准;
  域外问题拒答正确率 0.5 → 1.0。
- **上下文装配**:双门槛(作答门槛 + 上下文纳入门槛)、同文档块上限、来源标注与「★最相关来源」标记、
  多来源辨析提示。
- **文档解析**:pdf / docx / txt / md / csv;条款级切分(空行与「第X条/一、/1.」边界);
  表格结构化解析(docx 按正文顺序、PDF `find_tables`,逐行渲染为「字段:值」语句)。
- **引用溯源**:回答标注 `[n]` 引用编号,并给出源文件、制度编号、版本状态、条款路径与页码。

### 新增 — 评测与工程
- **可复现评测集**:10 篇制度语料 / 17 条标注问题(含易混淆、表格、版本干扰、域外拒答),
  四种检索模式消融(hit@1 / hit@3 / MRR)与答案指标(引用命中 / 事实命中 / 拒答正确率)。
- **回归体系**:端到端验收 19 项 + 三项创新 27 项 + 产品化增强 27 项 = 73 项断言。
- **稳定性工程**:守护脚本(后端崩溃 5 秒自愈、Ollama 宕机自动唤醒)、推理全局串行(防显存挤爆)、
  重 IO 端点线程池化、前端 SSE 超时与中断。
- **Web 界面**:免构建单页界面(知识库 / 文档 / 问答 / 自动分类建库 / 审计 / 系统状态),
  含冲突告警面板、版本链、隔离证据表、增量收益与制度到期提醒。

### 已知限制
- 扫描件 PDF 需自行接入 OCR(当前跳过并提示);
- 交叉编码器重排未引入(8GB 显存与生成模型冲突),以确定性元数据重排替代;
- 未实现知识图谱、多模态与外部数据源连接器(S3/Notion 等)。
